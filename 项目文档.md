* # OpenInsight 项目技术文档

  ## 1. 项目背景

  随着开源软件在全球范围内的蓬勃发展，GitHub 等代码托管平台上涌现了海量的开源项目。对于开发者而言，如何在众多的同类项目中进行技术选型；对于项目维护者而言，如何客观评估社区的健康度与活跃度；对于企业与投资人而言，如何发掘具有潜力的开源项目，都成为了亟待解决的问题。

  当前市场上的开源项目数据分析工具主要存在以下痛点：

  1.  **数据孤岛**：缺乏权威、全面的数据源，难以获取深度的项目元数据。
  2.  **评估维度单一**：往往仅关注 Star、Fork 等表层指标，无法反映项目的真实活跃度和协作效率。
  3.  **理解门槛高**：枯燥的数据报表难以转化为直观的业务洞察，缺乏能够解读数据的智能辅助工具。

  **OpenInsight** 正是在此背景下应运而生。它依托 X-lab OpenDigger 开源生态数据分析挖掘平台，旨在构建一个集多维评估、可视化展示、AI 智能分析于一体的开源项目洞察平台，为开源生态的各类参与者提供决策支持。

  ## 2. 产品定位

  OpenInsight 定位为 **"智能化开源项目全景洞察平台"**。

  *   **目标用户**：
      *   **开发者**：用于技术选型对比，快速了解项目活跃度与维护情况。
      *   **开源运营者**：监控社区健康指标，优化运营策略。
      *   **高校与研究机构**：基于数据进行开源生态学术研究。
  *   **核心价值**：
      *   **全貌还原**：通过 OpenRank、Activity 等深度指标还原项目真实影响力。
      *   **直观可视**：高性能交互式图表，让数据“会说话”。
      *   **智能辅助**：引入大模型 AI，将冷冰冰的数字转化为有温度的分析报告。

  ## 3. 系统功能架构

  系统主要包含数据采集层、业务逻辑层（API）和前端展示层，具体功能模块如下：

  ### 3.1 核心功能模块

  1.  **多维指标评估体系**
      *   **GitHub 指数**：综合 Influence, Reaction, Developer, Trend 四个维度生成的六维雷达图，全面画像。
      *   **PREI 指数**：专注于评估 Issue/PR 的响应与解决效率，反映维护团队的活跃度。
      *   **OpenRank**：基于协作网络的社会网络分析算法，准确衡量项目在全域生态中的影响力。

  2.  **可视化数据看板**
      *   实现各维度指标的趋势图、折线图、柱状图展示。
      *   支持日期范围筛选、数据粒度切换（月度/季度）。
      *   提供 Top 榜单功能，展示数据库中收录的最活跃项目。

  3.  **AI 智能分析报告**
      *   集成 DeepSeek、通义千问 (Qwen) 等主流 LLM。
      *   基于项目的实时指标数据，动态生成包含“项目现状”、“社区健康度”、“风险预警”的文本报告。
      *   支持导出 PDF 格式的分析报告。

  4.  **项目对比分析**
      *   支持将多个竞品项目加入对比池。
      *   横向对比各项目的各项核心指标，生成对比图表，辅助技术选型。

  5.  **后台管理系统**
      *   **任务调度**：可视化的 Cron 任务管理，监控数据同步与指标计算任务。
      *   **模型配置**：可以在后台动态配置使用的 AI 模型厂商、API Key 及 Prompt 参数。
      *   **系统监控**：查看系统运行日志与 API 调用状态。

  ## 4. 技术架构设计

  ### 4.1 技术栈

  | 模块         | 技术选型           | 说明                                     |
  | :----------- | :----------------- | :--------------------------------------- |
  | **前端**     | Vue 3 + TypeScript | 现代化的组件化开发框架，保证代码可维护性 |
  | **构建工具** | Vite               | 极速的开发服务器启动与模块热更新         |
  | **UI 框架**  | Ant Design Vue     | 企业级中后台 UI 组件库                   |
  | **可视化**   | Apache ECharts 5   | 高性能、功能丰富的图表库                 |
  | **状态管理** | Pinia              | 轻量级、类型安全的状态管理库             |
  | **后端**     | Node.js (Express)  | 轻量级 Web 框架，适合 I/O 密集型应用     |
  | **数据库**   | MySQL 8.0          | 存储业务数据（用户、配置、缓存指标）     |
  | **数据源**   | X-lab OpenDigger   | 提供底层的 ClickHouse 数据查询接口       |

  ### 4.2 架构分层

  1.  **展现层 (Presentation Layer)**:
      *   响应式布局设计，自动适配 PC (网格布局) 与 移动端 (流式布局)。
      *   虚拟列表 (Virtual Scroller) 技术优化长列表渲染性能。

  2.  **业务层 (Business Layer)**:
      *   RESTful API 设计风格。
      *   **ETL 服务**：负责从 OpenDigger 拉取原始数据 -> 清洗 -> 计算复合指标 -> 存入本地 MySQL。
      *   **AI 服务**：封装 LLM 接口，实现 Prompt 模板管理与流式响应处理。

  3.  **数据层 (Data Layer)**:
      *   MySQL 连接池管理。
      *   使用 Promise 封装数据库操作，保证异步流程的可控性。

  ## 5. 项目亮点

  1.  **极致的性能体验**：针对大量数据的图表渲染与列表展示进行了深度优化，确保在数据量较大的情况下页面依然流畅。
  2.  **灵活的 AI 适配**：后端采用策略模式设计 AI 调用模块，可以无缝切换不同的模型提供商（OpenAI, Qwen, DeepSeek 等），适应不同的成本与性能需求。
  3.  **自动化运维**：内置完善的定时任务系统，确保指标数据的自动更新与同步，降低了人工维护成本。
